package com.mu.game.model.guide.arrow;

public class AutoOrQuitDunArrow extends CommonArrow {
   public AutoOrQuitDunArrow(int arrowId, int pushId) {
      super(arrowId);
      this.pushId = pushId;
   }
}
